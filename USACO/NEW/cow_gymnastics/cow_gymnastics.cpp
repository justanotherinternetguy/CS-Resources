#include <bits/stdc++.h>
using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  freopen("./gymnastics.in", "r", stdin);
	freopen("./gymnastics.out", "w", stdout);

  int N, K;
  cin >> K >> N;

  for (int i = 0; i < K; i++) {
    for (in)
  }

  return 0;
}
