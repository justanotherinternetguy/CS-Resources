#include <bits/stdc++.h>
using namespace std;
#define ll long long;


int main() {
  freopen("shuffle.in", "r", stdin);
  freopen("shuffle.out", "w", stdout);
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);

  int n;
  cin >> n;
  int pos[n];
  int ids[n];

  int ans[n];

  for (int i = 0; i < n; i++) {
    cin >> pos[i];
  }

  for (int i = 0; i < n; i++) {
    cin >> ids[i];
  }


  
  
  return 0;
}
